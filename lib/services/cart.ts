import type { Cart } from "@/types/commerce";

/**
 * Servicio de carrito (placeholder).
 * Persistir en Supabase o cookies cuando el flujo esté implementado.
 */
export async function getCart(): Promise<Cart | null> {
  return null;
}
